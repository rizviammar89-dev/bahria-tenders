// Brand: Bahria Tenders uses a single light (cream/green/gold) theme — the mockup is light only.
// Ignore the device's dark-mode setting so the app always renders the brand cream background.
export function useColorScheme(): 'light' | 'dark' {
  return 'light';
}
