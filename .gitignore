# Node
node_modules/

# Expo
.expo/
dist/
*.apk
*.aab

# Supabase local
supabase/.temp/
supabase/.branches/

# Env & secrets — never commit; FCM V1 service-account JSON lives ONLY in EAS credentials
.env*
!.env.example
*service-account*.json
# Firebase admin SDK / service-account keys download as <project>-firebase-adminsdk-<hash>.json
*firebase-adminsdk*.json
*-adminsdk-*.json

# OS / editor
Thumbs.db
.DS_Store
